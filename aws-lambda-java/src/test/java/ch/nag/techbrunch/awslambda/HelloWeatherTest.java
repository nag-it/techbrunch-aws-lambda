package ch.nag.techbrunch.awslambda;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class HelloWeatherTest {

    @Test
    void handleRequest() {
//        WeatherData weatherData = new WeatherData("Basel", 20);
//        HelloWeather helloWeather = new HelloWeather();
//        WeatherData result = helloWeather.handleRequest(weatherData, new TestContext());
//        assertEquals("Basel", result.location());
//        assertEquals(22, result.temperature());
    }
}